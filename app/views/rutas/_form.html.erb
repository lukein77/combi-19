<%= form_for @ruta do |f| %>

	<%= f.label :ciudadOrigen %>
	<br>
	<%= f.collection_select :ciudad,
													:ciudad_id,
													@ciudades,
													:id,
													:nombre %>

	<%= f.label :ciudadDestino %>
	<br>
	<%= f.collection_select :ciudad,
													:ciudad_id,
													@ciudades,
													:id
													:nombre %>												

  <%# No funciona f.collection_select :ciudadDestino, @ciudades, :id, :nombre %>												
	<br> <br>										
  <%= f.submit "Guardar" %>
<% end %>