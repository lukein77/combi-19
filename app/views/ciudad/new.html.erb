<!-- Ciudad#new -->

<%= form_for(@ciudad) do |f| %>
	<%= f.label :nombre %>
	<%= f.text_field :nombre,
										required: true,
										label: "Nombre",
										placeholder: "Ingrese el nombre de la ciudad" %>
  <%= f.submit "Guardar" %>

<% end %>