<% if @viaje.errors.any? %>
	<h3> No se pudo guardar el viaje: </h3>
	<ul>
		<% @viaje.errors.messages.each do |msg| %>
			<li> <%= msg[1][0] %> </li>
		<% end %>
	</ul>
<% end %>

<%= form_for @viaje do |f| %>

  <div class="field">
    <%= f.label "Ruta" %>
    <%= f.collection_select(:ruta_id, @rutas, :id, :nombre) %>
  </div>

  <div class="field">
    <%= f.label "Combi" %>
    <%= f.collection_select(:combi_id, @combis, :id, :patente) %>
  </div>

  <div class="field">
    <%= f.label "Chofer" %>
    <%= f.collection_select(:chofer_id, @choferes, :id, :nombre) %>
  </div>

  <div class="field">
    <%= f.label "Precio" %>
    <%= f.number_field :precio, required: true, min: 0, step: 0.1, placeholder: "Ingrese el precio" %>
  </div>

  <!-- div class="field">
    < %= f.label "Fecha" %>
    < %= f.date_field :fecha, required: true, min: Date.today %>
  </div -->

  <div class="field">
    <%= f.label "Salida" %>
    <%= f.datetime_select :fecha_hora, required: true, minute_step: 5, start_year: Time.now.year %>
    <!-- COMPROBAR QUE NO CREE UN VIAJE EL MISMO DIA EN UNA HORA PASADA -->
  </div>

  <br> <br>                                       
  <%= f.submit "Guardar", class: 'btn btn-info btn-sm' %>
<% end %>

  <!-- PREVISUALIZO ORIGEN Y DESTINO DE LA RUTA -->
  <!-- p> Origen:  < %= #ruta.getCiudadOrigen %> - Destino: < %= #ruta.getCiudadDestino %> </p -->